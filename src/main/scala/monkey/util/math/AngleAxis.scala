package monkey.util.math

import monkey.util.extension.math.MathExt

/**
 * The axis should always be normalized!
 */

class AngleAxis extends MathExt:

  val angle = 0f
  val axis = V(1, 0, 0)
